for the sword
needs to be somewhere within the character

- constantly update a variable storing the players direction
    - loop that waits for button press (update function)

- set knife/sword position to a certain point depending on button press
    - left would be something like (0, -y)
    - right would be (x, y),
    - top (x, 0)
    - bottom (x, -y)